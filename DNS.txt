Nous avons observé qu'il y a une vingtaine de nom de domaine résolu lors de l'ouverture de l'application. (TODO : aussi pour quand appel quelqu'un savoir à qui se connecter ?)
Les serveurs autoritatifs (serveurs avec les info les plus récente) pour ces noms de domaines sont géré par l'entreprise MarkMonitor Inc qui est le registrar pour ces noms de domaine (TODO : pas sure que ce soit bon ça, est ce registrar gère le nom de domaine après ?). Un registrar est une entreprise chez qui ont peut aller acheter un nom de domaine (TODO : mieux expliquer ce qu'un regitrar ?)
Les noms de domaines résolu apartiennent à Microsoft. (TODO : peut-être check cette info pas sur d'avoir récupéré l'info correctement)
Les requêtes DNS effectué sont des requêtes de type standars, qui sont des requêtes utilisé lorsque l'on veut récupérer des données, ce qui fait du sens car l'on veut charger l'appliation et toutes ses données.
Toutes les requets utilisé ont été faite de manière récursive (TODO : explixer un petit peu (dernier paragraph p129 du sylla))
Les version d'adresse ipv4 ont l'air d'être préféré par skype. La majorité des demandes étaient des ipv4 (record A) et quelques une était demandé en ipv4 et ipv6 (une demande avec le record A et une avec le record AAAA). (TODO : explication de pq + de ipv4 et pq parfois demande pour les deux (j'ai vu quelque part que demander A et AAAA "normal when both stacks are bound" mais pas compris))
On a pu remarquer que les requêtes contenaient des records additionel (données qui sont fournit en supplément) qui servent à (TODO: chercher à quoi ça sert (à l'air de donner l'adresse IP des serveurs autoritatif ?) (apres certaine requête souvent la même qui suit du coup donne déjà l'info ?))
Il y a l'air d'avoir pas mal de retransmition majoritairement pour les querry (TODO : vraiment bcp ou quantité normal + explications (première piste : protocol UDP donc plus de rentransmition), parler du temps de retransmission choisi ? et si c'est vraiment des paquets perdu ou juste a pas attendu assez longtemps).
Nous avons remarquer que toute les réponses reçus on le record CNAME qui signifie que la requête est un alias. Mais aucune requête pour l'alias est faite après (TODO : expliquer pq, peut-être liens avec le schéma que tu avais trouvé)